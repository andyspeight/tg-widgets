/**
 * What the assistant is called.
 *
 * Andy chose "Luna Assist" on 16 Sep 2026 (docs/tg-sites-copilot-review.md,
 * section 9). The brief called it a copilot, which is Duda's word and GitHub's
 * and Microsoft's; the product family is Luna (Luna Chat, Luna Work), so the
 * name fits. It is one constant so a change of mind is a one-line change, and
 * nothing else in the code says the name.
 */

export const ASSISTANT_NAME = 'Luna Assist';

/** The short form, for a sentence that has already said the full name once. */
export const ASSISTANT_SHORT = 'Luna';
