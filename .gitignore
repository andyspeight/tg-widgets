# Dependencies — never commit these (a leaked node_modules has bitten this package before)
node_modules/
package-lock.json

# Playwright / test browser binaries
test-results/
playwright-report/

# OS noise
.DS_Store

# Staging area for one-off deliverables handed to the user directly (e.g. the
# C# reference port), not part of the widget platform source.
scratchpad-deliverables/
