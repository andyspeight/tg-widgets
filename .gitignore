# Dependencies — never commit these (a leaked node_modules has bitten this package before)
node_modules/
package-lock.json

# Playwright / test browser binaries
test-results/
playwright-report/

# OS noise
.DS_Store

# Impeccable: per-developer detector overrides (config.json is shared and committed)
.impeccable/config.local.json
# Coastwise seed by-products: previews are regenerated by render-coastwise.mjs,
# and the font SQL carries megabytes of hex the edge function now derives itself.
tg-sites/tools/coastwise-preview/
tg-sites/tools/coastwise-seed/fonts.sql
tg-sites/tools/coastwise-seed/font-stmts/
